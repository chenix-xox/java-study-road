server:
  port: 80

spring:
  application:
    name: cloud-consumer-order
  cloud:
    # consul 配置
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}